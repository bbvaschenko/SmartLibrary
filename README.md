# 📚 Умная Библиотека: Система Аналитического Поиска и Анализа Книг

## 🎯 Обзор проекта

**Умная Библиотека** — это интеллектуальная система для автоматизированного анализа, категоризации и семантического поиска книг, использующая современные технологии обработки естественного языка и агентную архитектуру. Система автоматически обрабатывает PDF-файлы, извлекает текст, генерирует теги, создает резюме и предоставляет персонализированные рекомендации через взаимодействие нескольких AI-агентов.

---

## ✨ Ключевые возможности

- 📄 **Автоматическая обработка PDF** — извлечение текста и метаданных
- 🏷️ **Семантическое тегирование** — автоматическая категоризация по 10+ параметрам
- 📊 **Глубокий анализ контента** — оценка релевантности, сложности, целевой аудитории
- 🤖 **Агентная архитектура** — 6 специализированных AI-агентов
- 🔍 **Интеллектуальный поиск** — поиск по тегам и семантический поиск
- 📝 **Авторезюмирование** — генерация детальных, кратких и академических резюме
- 📈 **Система метрик** — 6 объективных метрик качества работы системы
- 💡 **Персонализированные рекомендации** — адаптированные под уровень пользователя

---

## 🏗️ Архитектура системы

### Агентная структура:

| Агент | Роль | Статус |
|-------|------|---------|
| **CoordinatorAgent** | Главный координатор, стратег | ✅ Работает |
| **AnalysisAgent** | Глубокий анализ контента | ✅ Работает |
| **CriticAgent** | Контроль качества результатов | ✅ Работает |
| **RecommendationAgent** | Формирование рекомендаций | ✅ Работает |
| **SummaryAgent** | Генерация резюме | ✅ Работает |
| **SearchAgent** | Поисковая функциональность | ⚠️ Требует модернизации |

### Технологический стек:
- **Python 3.8+** — основной язык разработки
- **GigaChat API** — LLM для анализа и генерации
- **PyPDF2** — обработка PDF файлов
- **Pandas/OpenPyXL** — работа с Excel
- **Requests** — HTTP запросы к API
- **JSON/YAML** — конфигурация и логи

---

## 📁 Структура проекта

```
Ai-Books-Analysing-main/
├── agents/                          # AI-агенты системы
│   ├── gigachat_client.py          # Клиент для работы с GigaChat API
│   ├── analysis_agent.py           # Агент глубокого анализа
│   ├── coordinator_agent.py        # Главный координатор
│   ├── critic_agent.py             # Агент-критик контроля качества
│   ├── recommendation_agent.py     # Агент рекомендаций
│   ├── summary_agent.py            # Агент генерации резюме
│   └── search_agent.py             # Агент поиска(требует доработки)
│
├── utils/                          # Вспомогательные утилиты
│   ├── book_tagger.py              # Продвинутый теггер книг
│   ├── data_manager.py             # Менеджер данных (Excel + JSON)
│   ├── pdf_processor.py            # Обработчик PDF файлов
│   └── search_tags.py              # Поиск по тегам (ТРЕБУЕТ СОЗДАНИЯ)
│
├── uploads/                        # Директория для загрузки книг
├── perfect_pipeline_output/        # Результаты идеального пайплайна
├── pipeline_output/                # Результаты пайплайна
├── pipeline_cache/                 # Кэш для ускорения работы
│
├── .env                            # Конфигурационные переменные
├── book_tags.xlsx                  # База тегов книг (создается автоматически)
├── main.py                         # Главный скрипт запуска системы
├── perfect_pipeline.py            # Идеальный пайплайн с метриками
├── req.txt                        # Зависимости проекта
└── README.md                      # Документация
```

---

## ⚠️ Известные ограничения и проблемы

### некритические проблемы:
1. **Отсутствуют два ключевых модуля**:
   - `agents/search_agent.py` — агент поиска не доведён до конца

2. **Некорректная работа поиска** — существующая поисковая функциональность требует доработки

3. **Метрики не полностью реализованы** — в `perfect_pipeline.py`:
   - SCS (Self Consistency Score) — частично работает
   - ECS (Explanation Completeness Score) — требует доработки

4. **Дублирование кода** — `gigachat_client.py` присутствует в двух местах

### Функциональные ограничения:
- Анализ проводится только по метаданным (теги и резюме)
- Отсутствует обработка больших PDF файлов (>1000 страниц)
- Нет веб-интерфейса, только консольное приложение

---

## 🚀 Быстрый старт

### Шаг 1: Клонирование и настройка
```bash
# Клонируйте репозиторий
git clone <repository-url>
cd Ai-Books-Analysing-main

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r req.txt
```

### Шаг 2: Настройка GigaChat API
1. Зарегистрируйтесь на [GigaChat для разработчиков](https://developers.sber.ru/portal/products/gigachat)
2. Получите `CLIENT_ID` и `CLIENT_SECRET`
3. Создайте файл `.env` в корне проекта:
```env
GIGACHAT_CLIENT_ID=ваш_client_id_здесь
GIGACHAT_CLIENT_SECRET=ваш_client_secret_здесь
```

### Шаг 3: Создание необходимых директорий
```bash
mkdir -p uploads perfect_pipeline_output pipeline_output pipeline_cache
```

### Шаг 4: Запуск системы
```bash
# Основной интерфейс системы
python main.py

# Или запуск с расширенными метриками
python perfect_pipeline.py
```

---

## 🎮 Использование системы

### Интерфейс main.py:
```
🤖 УМНАЯ БИБЛИОТЕКА - Полная система
========================================

МЕНЮ:
1. Поиск книг
2. Загрузить новую книгу (PDF)
3. Полный анализ запроса
4. Статус системы
5. Выход
```

### Пример работы:
1. **Загрузите книгу** (PDF) → система автоматически:
   - Извлечет текст
   - Сгенерирует теги по 10+ категориям
   - Создаст детальное резюме
   - Сохранит в базу данных

2. **Выполните поиск** по темам, жанрам, сложности

3. **Получите анализ** с рекомендациями:
   - Релевантность теме
   - Уровень сложности
   - Целевая аудитория
   - Практические рекомендации

---

## 📊 Система метрик (Perfect Pipeline)

### A. АГЕНТНОСТЬ
| Метрика | Описание | Целевое значение | Статус |
|---------|----------|------------------|--------|
| **CER** | Эффективность CriticAgent | ≥0.2 | ✅ |
| **TID** | Разнообразие использования инструментов | ≥2 | ✅ |

### B. КАЧЕСТВО
| Метрика | Описание | Целевое значение | Статус |
|---------|----------|------------------|--------|
| **AAR** | Процент принятых ответов | ≥0.7 | ✅ |
| **SCS** | Самосогласованность результатов | ≥0.6 | ⚠️ |
| **ECS** | Полнота объяснений (1-5) | ≥4.0 | ⚠️ |

### C. НАДЕЖНОСТЬ
| Метрика | Описание | Целевое значение | Статус |
|---------|----------|------------------|--------|
| **FRR** | Способность к восстановлению ошибок | ≥0.5 | ✅ |

### Генерация отчета по метрикам:
```python
from perfect_pipeline import PerfectBookAnalysisPipeline

pipeline = PerfectBookAnalysisPipeline()
pipeline.export_metrics_report("отчет_по_метрикам.md")
```

---

## 🔧 Расширенная конфигурация

### Настройка теггера (book_tagger.py):
```python
# Параметры можно изменить в классе AdvancedBookTagger:
params = {
    'max_tags_per_category': 3,      # Макс. тегов на категорию
    'min_confidence': 0.05,          # Минимальная уверенность
    'top_n_overall': 10,             # Количество топ-тегов
    'word_window_size': 10,          # Размер окна для анализа
    'language': 'ru'                 # Язык анализа ('ru'/'en')
}
```

### Настройка пайплайна:
```python
# В perfect_pipeline.py:
pipeline = PerfectBookAnalysisPipeline(
    base_data_dir="uploads",          # Директория с книгами
    output_dir="analysis_results",    # Директория для результатов
    verify_ssl=False                  # Отключить SSL проверку (для тестов)
)
```

---

## 🐛 Поиск и устранение неисправностей

### Частые проблемы и решения:

**Проблема:** "ModuleNotFoundError: No module named 'agents'"
```bash
# Решение:
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
# или запускайте как модуль:
python -m main
```

**Проблема:** "Ошибка аутентификации GigaChat"
```python
# Решение: проверьте .env файл и попробуйте:
client = GigaChatClient(verify_ssl=False)
```

**Проблема:** "Нет результатов поиска"
1. Проверьте наличие данных в `book_tags.xlsx`
2. Убедитесь, что книга была успешно загружена
3. Попробуйте упростить поисковый запрос

**Проблема:** "Система работает медленно"
```python
# В pdf_processor.py уменьшите:
max_pages=100  # Уменьшите значение для больших файлов
```

---

## 📝 Примеры использования

### Пример 1: Полный цикл анализа
```python
from perfect_pipeline import PerfectBookAnalysisPipeline

# Инициализация
pipeline = PerfectBookAnalysisPipeline()

# 1. Загрузка книги
upload_result = pipeline.process_book_upload(
    pdf_path="uploads/математика.pdf",
    title="Учебник по высшей математике",
    generate_summary=True
)

# 2. Анализ запроса
analysis = pipeline.process_user_query(
    query="книги по дифференциальным уравнениям для студентов",
    enable_critique=True,
    max_iterations=3
)

# 3. Получение метрик
metrics = pipeline.get_metrics_summary()
print(f"Эффективность системы: {metrics['metrics']['AAR']:.1%}")
```

### Пример 2: Пакетная обработка
```python
from utils.book_tagger import AdvancedBookTagger

tagger = AdvancedBookTagger()
books = [
    {"text": text1, "title": "Книга 1"},
    {"text": text2, "title": "Книга 2"}
]

results = tagger.batch_analyze(books)
for result in results:
    print(f"Книга: {result[0]}")
    print(f"Теги: {result[3]}")
```

### Пример 3: Экспорт данных
```python
from utils.data_manager import DataManager

manager = DataManager()

# Экспорт всех данных
exported = manager.export_data("экспорт_данных")

# Поиск книг по критериям
results = manager.search_books({
    'academic_subjects': 'программирование',
    'complexity_level': 'начальный'
})
```

---

## 🚧 Требуемые доработки

### Высокий приоритет:
1. **Модернизация SearchAgent** — модификация агента поиска
2. **Исправить поисковую систему** — устранение ошибок поиска
3. **Доработать метрики SCS/ECS** — полная реализация 6 метрик

### Средний приоритет:
4. **Устранить дублирование кода** — объединить gigachat_client.py
5. **Добавить веб-интерфейс** — Flask/Django приложение
6. **Улучшить обработку ошибок** — устойчивость к большим файлам

### Низкий приоритет:
7. **Поддержка дополнительных форматов** — EPUB, DOCX, TXT
8. **Многопользовательский режим** — разделение по пользователям
9. **API для интеграции** — REST API для внешних систем

---

## 📊 Форматы данных

### Excel (book_tags.xlsx):
- **Структура:** 19 столбцов с метаданными книг
- **Формат тегов:** разделитель "|" (математика|физика|программирование)
- **Автоматическое обновление:** при каждой загрузке книги

### JSON файлы:
- **Метаданные системы:** `metadata.json`
- **Резюме книг:** `summary_{book_id}_{timestamp}.txt`
- **Логи системы:** `pipeline.log`, `agent_system_*.log`
- **Метрики:** `pipeline_metrics.json`

### Категории тегов:
1. **academic_subjects** — Академические дисциплины (15+ категорий)
2. **genres** — Жанры литературы (8+ категорий)
3. **time_periods** — Временные периоды
4. **audience** — Целевая аудитория
5. **complexity_levels** — Уровень сложности
6. **formats** — Форматы книг
7. **writing_styles** — Стили написания
8. **special_features** — Особые характеристики
9. **geographical_regions** — Географические регионы
10. **languages** — Языки книг

---

## 🔒 Безопасность

### Конфиденциальные данные:
- **API ключи GigaChat** — хранятся только в `.env` файле
- **Файлы книг** — хранятся локально, не передаются третьим сторонам
- **Аналитические данные** — анонимизируются при экспорте

### Рекомендации по безопасности:
1. Никогда не коммитьте `.env` файл в Git
2. Регулярно обновляйте API ключи
3. Используйте виртуальные окружения для изоляции зависимостей
4. Ограничьте доступ к директории `uploads`

---

## 🤝 Вклад в проект

### Требуемый вклад:
1. **Разработка недостающих модулей** — SearchAgent и search_tags.py
2. **Исправление поиска** — отладка существующей функциональности
3. **Доработка метрик** — полная реализация SCS и ECS
4. **Тестирование** — на различных типах книг и запросов
5. **Документация** — улучшение документации и примеров

### Процесс разработки:
```bash
# 1. Форкните репозиторий
# 2. Создайте ветку для фичи
git checkout -b feature/search-agent

# 3. Внесите изменения
# 4. Протестируйте
python test_pipeline.py

# 5. Создайте pull request
```

---

## 📞 Поддержка и обратная связь

### Полезные команды для отладки:
```bash
# Проверка структуры проекта
find . -name "*.py" | head -20

# Проверка зависимостей
pip list | grep -E "(requests|pandas|PyPDF2)"

# Мониторинг логов
tail -f pipeline.log

# Тест подключения к GigaChat
python test_key.py
```

### Часто задаваемые вопросы:

**Q: Где взять API ключи GigaChat?**
A: Зарегистрируйтесь на [developers.sber.ru](https://developers.sber.ru) и создайте приложение в GigaChat API.

**Q: Как добавить поддержку английского языка?**
A: В `book_tagger.py` измените параметр `language` на `'en'` и обновите таксономию.

**Q: Можно ли анализировать книги на других языках?**
A: Да, система поддерживает любой язык, но таксономию тегов нужно адаптировать.

**Q: Как увеличить скорость обработки?**
A: Уменьшите `max_pages` в pdf_processor.py и используйте кэширование.

---

## 📄 Лицензия

Проект разработан для исследовательских целей. Использование GigaChat API регулируется [условиями сервиса SberDevices](https://developers.sber.ru/docs/ru/gigachat/api/overview).

### Версия системы:
- **Текущая версия:** 1.0.0
- **Дата релиза:** 2025
- **Статус:** Альфа (требует доработки)

---

## 🎉 Заключение

**Умная Библиотека** представляет собой мощную основу для создания интеллектуальных систем анализа книг. Несмотря на текущие ограничения, система демонстрирует высокий потенциал в следующих областях:

- 📚 **Библиотечное дело** — автоматическая каталогизация
- 🎓 **Образование** — подбор учебных материалов
- 🔬 **Исследования** — анализ научной литературы
- 💼 **Бизнес** — анализ деловой литературы

### Дорожная карта развития:
1. ✅ Базовая функциональность (реализовано)
2. 🔄 Исправление поиска и метрик (в процессе)
3. ⏳ Веб-интерфейс и API
4. 🎯 Промышленное использование


---
*Последнее обновление: 2025 | Система Умная Библиотека*
